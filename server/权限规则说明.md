# 权限规则说明文档

## 概述

本文档描述了系统中所有的权限规则（Permission Code），用于RBAC权限控制系统。

## 权限规则命名规范

权限规则采用 `模块:资源:操作` 的三级命名规范：
- **模块**：系统模块，如 `sys`（系统管理）、`base`（基础业务）
- **资源**：具体资源，如 `user`（用户）、`role`（角色）、`group`（群组）
- **操作**：CRUD操作，如 `view`（查看）、`add`（新增）、`edit`（编辑）、`delete`（删除）、`assign`（分配）

## 系统管理权限 (sys:*)

### 用户管理 (sys:user:*)

| 权限代码 | 说明 | 描述 |
|---------|------|------|
| `sys:user:view` | 用户查看 | 查看用户列表、用户详情 |
| `sys:user:add` | 用户新增 | 注册新用户 |
| `sys:user:edit` | 用户编辑 | 更新用户信息 |
| `sys:user:delete` | 用户删除 | 删除用户 |
| `sys:user:assign` | 用户分配 | 给用户分配群组 |

**涉及的API接口**：
- `GET /api/user/list` - 需要 `sys:user:view`
- `GET /api/user/:id` - 需要 `sys:user:view`
- `PUT /api/user/:id` - 需要 `sys:user:edit`
- `DELETE /api/user/:id` - 需要 `sys:user:delete`
- `POST /api/user/assign-group` - 需要 `sys:user:assign`
- `POST /api/user/register` - 公开接口，无需权限
- `POST /api/user/login` - 公开接口，无需权限

### 角色管理 (sys:role:*)

| 权限代码 | 说明 | 描述 |
|---------|------|------|
| `sys:role:view` | 角色查看 | 查看角色列表、角色详情 |
| `sys:role:add` | 角色新增 | 创建新角色 |
| `sys:role:edit` | 角色编辑 | 更新角色信息 |
| `sys:role:delete` | 角色删除 | 删除角色 |
| `sys:role:assign` | 角色分配 | 给角色分配权限 |

**涉及的API接口**：
- `GET /api/role` - 需要 `sys:role:view`
- `GET /api/role/:id` - 需要 `sys:role:view`
- `POST /api/role` - 需要 `sys:role:add`
- `PUT /api/role/:id` - 需要 `sys:role:edit`
- `DELETE /api/role/:id` - 需要 `sys:role:delete`
- `POST /api/role/:roleId/permissions` - 需要 `sys:role:assign`

### 权限管理 (sys:permission:*)

| 权限代码 | 说明 | 描述 |
|---------|------|------|
| `sys:permission:view` | 权限查看 | 查看权限列表、权限详情 |
| `sys:permission:add` | 权限新增 | 创建新权限 |
| `sys:permission:edit` | 权限编辑 | 更新权限信息 |
| `sys:permission:delete` | 权限删除 | 删除权限 |

**涉及的API接口**：
- `GET /api/permission` - 需要 `sys:permission:view`
- `GET /api/permission/:id` - 需要 `sys:permission:view`
- `POST /api/permission` - 需要 `sys:permission:add`
- `PUT /api/permission/:id` - 需要 `sys:permission:edit`
- `DELETE /api/permission/:id` - 需要 `sys:permission:delete`

### 群组管理 (sys:group:*)

| 权限代码 | 说明 | 描述 |
|---------|------|------|
| `sys:group:view` | 群组查看 | 查看群组列表、群组树、群组详情 |
| `sys:group:add` | 群组新增 | 创建新群组、根群组 |
| `sys:group:edit` | 群组编辑 | 更新群组信息 |
| `sys:group:delete` | 群组删除 | 删除群组 |

**涉及的API接口**：
- `GET /api/group/root/:tenantId` - 需要 `sys:group:view`
- `GET /api/group/list/:tenantId` - 需要 `sys:group:view`
- `GET /api/group/tree/:tenantId` - 需要 `sys:group:view`
- `GET /api/group/tree/:tenantId/:groupId` - 需要 `sys:group:view`
- `GET /api/group/:id` - 需要 `sys:group:view`
- `POST /api/group/root` - 需要 `sys:group:add`
- `POST /api/group` - 需要 `sys:group:add`
- `PUT /api/group/:id` - 需要 `sys:group:edit`
- `DELETE /api/group/:id` - 需要 `sys:group:delete`

### 租户管理 (sys:tenant:*)

| 权限代码 | 说明 | 描述 |
|---------|------|------|
| `sys:tenant:view` | 租户查看 | 查看租户列表、租户详情 |
| `sys:tenant:add` | 租户新增 | 创建新租户 |
| `sys:tenant:edit` | 租户编辑 | 更新租户信息 |
| `sys:tenant:delete` | 租户删除 | 删除租户 |

**涉及的API接口**：
- `GET /api/tenant` - 需要 `sys:tenant:view`
- `GET /api/tenant/:id` - 需要 `sys:tenant:view`
- `POST /api/tenant/create` - 需要 `sys:tenant:add`
- `PUT /api/tenant/:id` - 需要 `sys:tenant:edit`
- `DELETE /api/tenant/:id` - 需要 `sys:tenant:delete`

## 基础业务权限 (base:*)

### 动态表管理 (base:table:*)

| 权限代码 | 说明 | 描述 |
|---------|------|------|
| `base:table:view` | 表查看 | 查看表列表、表详情 |
| `base:table:add` | 表新增 | 创建新表 |
| `base:table:edit` | 表编辑 | 更新表信息 |
| `base:table:delete` | 表删除 | 删除表 |

**涉及的API接口**：
- `GET /api/base/tables` - 需要 `base:table:view`
- `GET /api/base/tables/:tableId` - 需要 `base:table:view`
- `POST /api/base/tables` - 需要 `base:table:add`
- `PUT /api/base/tables/:tableId` - 需要 `base:table:edit`
- `DELETE /api/base/tables/:tableId` - 需要 `base:table:delete`

### 字段管理 (base:field:*)

| 权限代码 | 说明 | 描述 |
|---------|------|------|
| `base:field:view` | 字段查看 | 查看字段列表、字段详情 |
| `base:field:add` | 字段新增 | 创建新字段 |
| `base:field:edit` | 字段编辑 | 更新字段信息 |
| `base:field:delete` | 字段删除 | 删除字段 |

**涉及的API接口**：
- `GET /api/base/tables/:tableId/fields` - 需要 `base:field:view`
- `GET /api/base/tables/:tableId/fields/:fieldId` - 需要 `base:field:view`
- `POST /api/base/tables/:tableId/fields` - 需要 `base:field:add`
- `PUT /api/base/tables/:tableId/fields/:fieldId` - 需要 `base:field:edit`
- `DELETE /api/base/tables/:tableId/fields/:fieldId` - 需要 `base:field:delete`

### 记录管理 (base:record:*)

| 权限代码 | 说明 | 描述 |
|---------|------|------|
| `base:record:view` | 记录查看 | 查看记录列表、记录详情 |
| `base:record:add` | 记录新增 | 创建新记录 |
| `base:record:edit` | 记录编辑 | 更新记录信息 |
| `base:record:delete` | 记录删除 | 删除记录 |

**涉及的API接口**：
- `POST /api/base/tables/:tableId/records/list` - 需要 `base:record:view`
- `GET /api/base/tables/:tableId/records/:recordId` - 需要 `base:record:view`
- `POST /api/base/tables/:tableId/records` - 需要 `base:record:add`
- `PUT /api/base/tables/:tableId/records/:recordId` - 需要 `base:record:edit`
- `DELETE /api/base/tables/:tableId/records/:recordId` - 需要 `base:record:delete`

## 特殊角色

### 超级管理员 (super_admin)

角色代码：`super_admin`

超级管理员拥有系统中所有权限，可以跳过所有权限校验，直接访问所有API接口。

**特点**：
- 拥有所有 `sys:*` 权限
- 拥有所有 `base:*` 权限
- 可以访问所有需要权限的接口
- 适用于系统管理员或超级用户

### 管理员 (admin)

角色代码：`admin`

管理员拥有所有权限（通过角色-权限关联），与超级管理员类似，但需要显式配置权限。

## 权限校验流程

```
1. 用户发送请求
   ↓
2. authMiddleware 验证 Token，获取用户信息
   ↓
3. hasPermission 中间件检查权限
   ↓
4. 查询用户角色列表
   ↓
5. 检查是否有超级管理员角色（roleCode: super_admin）
   ├─ 是 → 直接放行，执行后续逻辑
   └─ 否 → 继续检查
   ↓
6. 查询角色关联的权限
   ↓
7. 检查是否有所需的权限代码
   ├─ 有 → 放行，执行后续逻辑
   └─ 无 → 返回 403 权限不足
```

## 权限设计原则

1. **最小权限原则**：只为用户分配完成工作所需的最小权限集
2. **职责分离原则**：敏感操作（如删除、分配）应分配给不同角色
3. **默认拒绝原则**：未明确授权的权限默认拒绝访问
4. **层级管理原则**：使用群组树结构实现权限的继承和隔离

## 创建权限的示例

在数据库中创建权限的SQL（通过Prisma）：

```typescript
// 创建用户查看权限
await prisma.permission.create({
  data: {
    permName: '用户查看',
    permCode: 'sys:user:view',
    type: 2, // 按钮类型
    sort: 1
  }
})
```

## 最佳实践

1. **使用角色而非直接分配权限**：将权限打包到角色中，然后分配角色给用户
2. **定期审查权限**：定期检查用户权限是否符合最小权限原则
3. **记录权限变更**：记录所有权限的创建、修改、删除操作
4. **权限缓存**：使用Redis缓存权限数据，提高校验性能
5. **日志审计**：记录所有权限相关的操作，便于审计追踪
